import { CustomeFilterPipePipe } from './custome-filter-pipe.pipe';

describe('CustomeFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
